[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tg-digest"
version = "0.1.0"
dependencies = [
    "telethon",
    "python-dotenv",
    "pyyaml",
]

[tool.setuptools.packages.find]
where = ["src"]
